{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <div class="row align-items-center mb-5">
        <div class="col-md-3">
            <img src="{{ asset('assets/header.jpg') }}" class="img-fluid rounded-3 shadow-sm" alt="Muzieklijstjes Header">
        </div>
        <div class="col-md-9">
            <h1 class="display-5 fw-bold">Welkom bij Muzieklijstjes</h1>
            <p class="fs-4">Ontdek en verken de beste muziekalbums zoals gerangschikt door critici en tijdschriften. Duik in samengestelde lijsten, vind je favoriete artiesten en verken de muziekgeschiedenis.</p>
            <p class="small text-muted">
                Deze website bevat recensies en lijsten van onder andere: 
                {% for source in sources %}
                    <a href="{{ path('app_magazine_show_by_name', {name: source}) }}" class="text-muted">{{ source }}</a>{% if not loop.last %}, {% endif %}
                {% endfor %}
            </p>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>Top van 2025</h3>
            <p class="text-muted">Gebaseerd op alle lijsten uit 2025</p>
            <div class="list-group">
                {% for item in aggregated2025Albums %}
                    {% set album = item.album %}
                    <a href="{{ path('app_album_show', {id: album.id, slug: album.slug}) }}" class="list-group-item list-group-item-action">
                        <div class="d-flex align-items-center">
                            {% if album.imageUrl %}
                                <div class="flex-shrink-0 me-3">
                                    <img src="{{ album_cover_base_url ~ album.imageUrl }}" alt="{{ album.title }}" class="img-fluid rounded" style="width: 60px; height: 60px; object-fit: cover;" loading="lazy">
                                </div>
                            {% else %}
                                <div class="flex-shrink-0 me-3 d-flex align-items-center justify-content-center bg-light rounded" style="width: 60px; height: 60px;">
                                    <span class="text-muted small">No Img</span>
                                </div>
                            {% endif %}
                            
                            <div class="flex-grow-1">
                                <h5 class="mb-1">{{ album.title }} <small class="text-muted d-block d-sm-inline">by {{ album.artist.name }}</small></h5>
                                {% if album.releaseYear %}
                                    <small>{{ album.releaseYear }}</small>
                                {% endif %}
                            </div>

                            <div class="flex-shrink-0 ms-2">
                                <span class="badge bg-primary rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px;">{{ item.score }}</span>
                            </div>
                        </div>
                    </a>
                {% else %}
                    <p class="text-muted">Nog geen data voor 2025.</p>
                {% endfor %}
            </div>
        </div>

        <div class="col-md-6">
            <h3>Muziekcanon</h3>
            <p class="text-muted">Gebaseerd op de belangrijke lijsten</p>
            <div class="list-group">
                {% for item in aggregatedImportantAlbums %}
                    {% set album = item.album %}
                    <a href="{{ path('app_album_show', {id: album.id, slug: album.slug}) }}" class="list-group-item list-group-item-action">
                        <div class="d-flex align-items-center">
                            {% if album.imageUrl %}
                                <div class="flex-shrink-0 me-3">
                                    <img src="{{ album_cover_base_url ~ album.imageUrl }}" alt="{{ album.title }}" class="img-fluid rounded" style="width: 60px; height: 60px; object-fit: cover;" loading="lazy">
                                </div>
                            {% else %}
                                <div class="flex-shrink-0 me-3 d-flex align-items-center justify-content-center bg-light rounded" style="width: 60px; height: 60px;">
                                    <span class="text-muted small">No Img</span>
                                </div>
                            {% endif %}
                            
                            <div class="flex-grow-1">
                                <h5 class="mb-1">{{ album.title }} <small class="text-muted d-block d-sm-inline">by {{ album.artist.name }}</small></h5>
                                {% if album.releaseYear %}
                                    <small>{{ album.releaseYear }}</small>
                                {% endif %}
                            </div>
                            
                            <div class="flex-shrink-0 ms-2">
                                <span class="badge bg-primary rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px;">{{ item.score }}</span>
                            </div>
                        </div>
                    </a>
                {% endfor %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
