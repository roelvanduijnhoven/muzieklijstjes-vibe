{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <div class="row align-items-center mb-5">
        <div class="col-md-3">
            <img src="{{ asset('assets/header.jpg') }}" class="img-fluid rounded-3 shadow-sm" alt="Muzieklijstjes Header">
        </div>
        <div class="col-md-9">
            <h1 class="display-5 fw-bold">Welkom bij Muzieklijstjes</h1>
            <p class="fs-4">Ontdek en verken de beste muziekalbums zoals gerangschikt door critici en tijdschriften. Duik in samengestelde lijsten, vind je favoriete artiesten en verken de muziekgeschiedenis.</p>
        </div>
    </div>

    <h1>Albumlijsten</h1>
    <div class="list-group">
        {% for list in lists %}
            <a href="{{ path('app_list_show', {id: list.id}) }}" class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">{{ list.title }}</h5>
                    {% if list.releaseYear %}
                        <small>{{ list.releaseYear }}</small>
                    {% endif %}
                </div>
                
                {% if list.type == 'aggregate' %}
                    <span class="badge bg-primary rounded-pill">Verzamelijst</span>
                {% endif %}
                {% if list.magazine %}
                    <span class="badge bg-secondary">{{ list.magazine.name }}</span>
                {% endif %}
                {% if list.code %}
                    <small class="text-muted ms-2">{{ list.code }}</small>
                {% endif %}
            </a>
        {% endfor %}
    </div>
    
    <div class="mt-3">
        {% if page > 1 %}
            <a href="{{ path('app_home', {page: page - 1}) }}" class="btn btn-secondary">Vorige</a>
        {% endif %}
        <a href="{{ path('app_home', {page: page + 1}) }}" class="btn btn-secondary">Volgende</a>
    </div>
</div>
{% endblock %}
